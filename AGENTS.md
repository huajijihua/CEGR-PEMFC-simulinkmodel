# PEMFC-cEGR 项目 Codex 补充规则

本仓库是阴极尾气循环 PEMFC 系统建模与验证工作区。Codex 的职责是先明确研究问题、模型边界、保真度和验证方式，再选择 MATLAB/Simulink、COMSOL 或两者协同的最小足够路线。

## 建模指导思想与路线总纲

1. 本项目默认以系统级 PEMFC-cEGR 模型为主线，高保真 COMSOL 模型用于局部机理、几何、多物理场、边界条件和关键部件校核。
2. MATLAB/Simulink 负责系统动态、控制逻辑、参数扫描、优化、拟合、数据处理和结果审计；COMSOL 负责空间分布、多物理场和局部响应。
3. 三方协同时必须先固定接口：参数名、单位、输入工况、边界变量、输出变量、探针/派生值、模型版本、数据格式和验收指标。
4. 不用 COMSOL 替代系统级主模型，也不用 Simulink 经验拟合替代高保真机理；两者通过参数、边界、探针、代理模型或降阶模型互相支撑。
5. 建模推进顺序优先为问题定义、系统级基线、局部高保真校核、参数/代理/ROM 回灌、系统级复验和风险边界说明。

## 模型边界

1. 当前主线是系统级 PEMFC-cEGR Simulink 模型，不把任务降格为孤立经验公式堆叠。
2. 修改前必须说明目标模型、目标子系统、工况、输入输出、控制量、验收指标和禁止修改范围。
3. 车载模型、台架模型、简化台架模型要分清，不把一个分支的参数或结论直接覆盖到另一个分支。

## 物理一致性

1. 电压模型应保留可逆电压、欧姆、活化和浓差/传质极化的语义。
2. 流量、组分、压力、湿度和热量模块必须保留质量守恒、能量守恒和变量单位语义。
3. 数据拟合只用于参数校准或经验插值，不能替代核心物理机制；若低误差拟合破坏物理敏感性，应标记为不合格或限制用途。
4. 公式、参数和边界条件应能追溯到实验数据、资料说明、文献、书籍摘录或成熟模型案例。

## 工具与验证

1. 读取模型优先使用 `model_overview`、`model_read(depth=0/1)`、`model_query_params` 和 `model_resolve_params`。
2. 结构或参数修改优先使用 `model_edit`、MATLAB/Simulink API 或受控脚本，不直接修改 `.slx` 内部文件。
3. 结构性修改后优先运行 `model_check`，再做 read-back verification。
4. 行为验证使用最小必要工况、已有脚本、`sim()`、`model_test` 或 MATLAB 单元测试。
5. 无法闭环验证时，必须在结论中明确写出未验证原因和剩余风险。

## COMSOL 自动建模工作流

1. 本机 COMSOL 入口固定为 `D:\COMSOL63\Multiphysics`；GUI 使用 `bin\win64\comsol.exe`，Server 使用 `bin\win64\comsolmphserver.exe`，MATLAB LiveLink 使用 `mli`，默认端口 `2036`。
2. 本项目 COMSOL 模型位于 `02_多物理场机理模型演示`。不得直接按二进制、XML 或文本方式修改 `.mph`；必须通过 COMSOL GUI、COMSOL API、MATLAB LiveLink 或受控脚本访问。
3. COMSOL 任务先区分新建模型、增量修改、模型审查和仿真验证。新建模型先形成模型规格和构建计划；增量修改先只读盘点当前模型；模型审查只输出证据链、风险和建议，不默认修改。
4. 自动建模前必须明确几何抽象、物理场、耦合关系、边界条件、材料、参数、输入工况、输出指标、验证方式、允许修改范围和禁止修改范围。
5. 默认采用共享 server 会话：用户在 GUI 中打开目标 `.mph`，Codex 连接同一个 `localhost:2036` COMSOL Server。默认由 GUI 检查并保存模型，后台脚本不得擅自保存 `.mph`。
6. Codex 必须先只读识别当前 server 中的模型 tag、文件标签、组件、几何、材料、物理场、变量、探针、selection、study、solver 和 solution；不得假设 `comp1/geom1` 等默认标签可靠。
7. 自动建模采用小步闭环：创建/修改几何、selection、材料、physics feature、mesh、study 或 solver 后，立即做 read-back verification，记录真实 API 路径和值。
8. 涉及边界条件、入口出口、反应、源项、材料或初始值时，必须追溯到具体 feature 节点和 API 路径；父 physics 节点设置不能替代完整方程或边界证据。
9. 求解验证只运行最小必要 study、局部检查或已有验证脚本，不默认重算全部工况；无法求解时必须区分几何/网格/物理/初值/求解器问题。
10. 同一模型默认只允许一条 Codex 控制链路，避免多个 Python/mph、MATLAB LiveLink 或后台脚本并发修改同一 server 会话。

## MATLAB/Simulink-COMSOL 协同自动建模

1. 协同任务先形成 `interface_contract`，明确 MATLAB/Simulink 传给 COMSOL 的参数、工况、边界条件，以及 COMSOL 返回的探针、派生值、场量摘要或代理模型参数。
2. 优先用 Simulink 完成快速系统级扫描和控制策略筛选，只把强敏感、强耦合、强约束或需要空间分布证据的局部问题交给 COMSOL。
3. COMSOL 结果回灌到系统级模型时必须保留单位、适用范围、插值/拟合边界、误差指标和物理解释，不能只回灌黑箱系数。
4. 协同验证至少包含接口读回、单工况 smoke test、关键 KPI 对照和失败栈摘要；长时间批量计算必须先通过小样本闭环。
5. Codex 输出压缩为接口表、模型变更摘要、KPI、证据路径、误差指标和剩余风险。

## AMEsim 协同说明

1. 后续 AMEsim 正式建模主工作区为 `E:\agentwork_AMEsim_0625`，本项目中的 `03_AMEsim系统模型` 不作为 AMEsim 主开发目录。
2. 本项目需要 AMEsim 协同时，只引用 `E:\agentwork_AMEsim_0625` 中的模型资产、接口结果、KPI 摘要和审计报告。
3. AMEsim 结果回灌到 PEMFC-cEGR 系统模型时，必须附单位、适用范围、误差指标、变量语义和物理解释。

## Token 与产物控制

1. 不默认导出图片、CSV、报告、模型副本或大批量中间文件。
2. 必须生成中间产物时，放入任务专用输出目录，并使用可识别命名。
3. MATLAB 本地完成计算和筛选，只向 Codex 返回 KPI、摘要、失败栈、关键变量和必要证据。
4. 不把完整日志、完整 timeseries、完整模型树、Simulink 缓存或工作区 dump 塞进上下文。
5. 任务结束前说明保留了哪些生成文件及用途；无保留价值的临时文件应清理或请求确认后清理。
6. COMSOL 任务不默认回传完整模型树、完整 Java dump、完整变量表、完整求解日志或批量导出图；只回传 KPI、必要错误栈、关键变量、证据路径和结论。
