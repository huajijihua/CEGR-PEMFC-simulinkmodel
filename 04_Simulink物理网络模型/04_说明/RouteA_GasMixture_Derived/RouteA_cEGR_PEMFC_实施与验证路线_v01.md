# RouteA cEGR-PEMFC 实施与验证路线 v01

状态：Phase 1-3 的工作树盘点、规格草案和资产归档已完成；Phase 4 六批顶层规划已与用户共同对齐；Stage 1 控制接口和九工况稳态基线已完成。原 120 s 和冷态起算的 600 s 运行仅保留为暖机/架构 smoke 证据；当前已冻结一套 `j=0.1 A/cm^2`、零实际回流的正常运行全状态初值，并完成该初值下恒 28 A 的 `0 -> 0.10 -> 0` 代表性瞬态、四处气体条件和 N2/O2 守恒首轮读回。后续稳态性能统一使用固定研究时长的末段均值和时间对齐的零回流参考，不将周期运行系统误写为严格静态点。恒流 cEGR 比矩阵、恒压接口和真实物理快速隔离继续待实施。
原则：本路线的 Phase 1-4 不修改 `.slx`、不调整 `platform_default`、不新增物理部件或控制律。它们的目的，是把当前 Route A 从“可运行的官方派生模型”明确为“面向工程化系统模型的受控起点”；Phase 4 的产物是与用户共同确认的后续建模顶层规划，不自动触发任何 Simulink 改动。

已完成的 `run_routeA_phase1_matching_audit.m`、九工况结果和参数/接口账本属于既有基线证据，不是本路线的 Phase 编号。它们可用于现状判断，但不得替代后续设备参数、部件能力或独立验证证据。

## 0. 顶层规划总览

### 0.1 最终建模目标

Route A 的长期目标不是完成一次工况计算，而是形成一套以 Simulink/Simscape 为核心的、可复用的工程化燃料电池系统集成设计与参数匹配平台。平台应以电堆为核心，组织供氧与 cEGR、供氢与阳极水管理、热管理、电气接口以及控制/观测架构，在系统级完成物质、能量、压力、湿度、温度、电气和控制耦合的计算。

模型的最终价值体现在四个方面：

1. 能根据电堆需求和边界条件完成 BOP 能力匹配，而不是孤立调节模块参数。
2. 能用同一个模型和参数体系研究无/低/中 cEGR、低/中/高负载稳态以及代表性切换瞬态。
3. 能通过模块化边界、统一接口和参数注册表持续替换或升级设备模型，而不破坏其他子系统。
4. 能形成可追溯的参数来源、能力范围、验证证据和后续标定路径，为进一步研究提供稳定系统级基础。

### 0.2 最终模型形态

最终模型是复杂的零维集总参数系统级物理网络，不追求在 Simulink 内求解几何空间场分布。它由以下层次组成：

- **核心设备层**：电堆 MEA、气道、反应、电压、热和电气端口。
- **辅助系统层**：供氧/cEGR、供氢/阳极水管理、热管理和电气接口四域。
- **控制与观测层**：负载、空气、cEGR、压力、湿度、温度、热管理和诊断控制。
- **参数与研究层**：`platform_default`、显式研究场景、派生量、`external_case`、参数优先级和验证矩阵。

各层通过官方物理网络、守恒/本构方程、物理端口、信号接口和参数契约耦合。MATLAB MCP/Simulink 工具用于模型读回、参数核查、结构检查和仿真验证；它是实施和验证手段，不替代顶层模型定义。

### 0.3 长期实施路径与预期成果

Stage 0 已由 Phase 1-4 完成；以下 Stage 1-5 是后续系统模型演进的长期工作包。每个阶段都必须同时回答“研究什么、改什么、依据什么、如何验证、产出什么”。

#### Stage 1：电堆核心与供氧/cEGR 联合匹配

- **目标**：以电堆反应、功率、电压和热需求为锚点，闭合空气供给、氧计量、cEGR 回流、阴极压力/RH 和相关控制边界。
- **系统对象**：电堆 MEA/气道/热电端口、空气源与压缩机接口、中冷/加湿接口、cEGR 阀与管路、排气/背压、水分离、入口混合、测量和空气/cEGR 控制链。
- **核心问题**：电堆需求是否能推导出有限且量纲正确的空气和回流需求；实际 `lambda_ca_in` 是否满足门槛；回流目标是否由正确压差和阀工作区间实现；压缩机请求是否在地图有效范围内。
- **重点参数**：电池数量、活性面积、负载/电流关系、反应与热电需求、空气质量流量和组分、压力/RH/温度边界、cEGR 比值、阀面积/压差、背压和加湿控制。只将影响闭合或验收的 `P0/P1` 参数纳入主动研究。
- **接口与场景**：保持 `u/w/y/z` 契约；先做端口级读回和中负载 `noCEGR/0`、`withCEGR/0.10`、`withCEGR/0.30`，再扩展低/高负载稳态，最后做 `0 -> 0.10 -> 0` 瞬态。
- **实施路径**：来源核查和参数账本 → MATLAB MCP 只读模型/端口/参数读回 → 识别真实缺口 → 必要时只对一个物理边界做小步封装、修复或增强 → `model_check`、端口读回和最小回归。
- **阶段产出**：电堆-BOP 联合能力账本、端口级接口表、模块来源/缺口表、代表性工况回归证据和下一阶段热管理输入。
- **退出条件**：电堆和供氧/cEGR 的质量、组分、压力、湿度、热量和控制关系闭合；无 cEGR/有 cEGR/切换场景满足现行门槛；未具备设备数据的能力明确标为 L2 或 `deferred`。

#### Stage 2：热管理参数匹配与热控制闭合

- **目标**：把电堆反应热、冷却回路、堆温目标、环境换热和热控制纳入与气体系统一致的能力匹配。
- **系统对象**：电堆热端口和冷却通道、冷却泵、冷却液边界、散热器/环境换热、节温或旁通控制、必要时 PTC 和温度测量。
- **核心问题**：电堆热负荷能否由冷却流量和温差带走；泵和回路压降是否匹配；散热能力是否有 UA/风量或等价证据；堆温在低/中/高负载和 cEGR 变化下是否稳定。
- **重点参数**：反应热、冷却液物性、流量、`DeltaT`、泵特性、回路压降、散热器 UA/风量、环境边界、堆温目标和热控制参数。缺少泵/换热数据时只保留工程量级和 L2 状态。
- **接口与场景**：输入承接 Stage 1 的电堆热负荷和负载轨迹；输出堆温、冷却流量、进出口温度、热量和执行器饱和；优先验证低/中/高负载稳态，再验证负载阶跃或 cEGR 切换引起的热响应。
- **实施路径**：热量账本 → 热液网络和参数来源读回 → 冷却能力/温差/环境换热闭合 → 只在证据支持时升级泵、换热或控制模块 → 热平衡和动态回归。
- **阶段产出**：冷却能力边界、堆温控制参数、热量平衡证据、散热能力缺口和与供氧/cEGR 的热耦合接口。
- **退出条件**：目标工况下热量守恒有限，冷却需求与设备能力范围一致，堆温和执行器约束通过；无 UA、风量或泵数据的结论保持为待证，不宣称真实散热能力。

#### Stage 3：供氢与阳极水管理参数匹配

- **目标**：形成氢源、减压/喷射、阳极回流、排氮、液水分离和 purge 控制的系统级闭合，并与电堆氢耗和压力需求匹配。
- **系统对象**：氢源和减压接口、比例阀/喷射器、循环泵或引射器、阳极气道和容腔、分离器、purge 阀、压力/流量/组分/液水测量及相关控制。
- **核心问题**：氢耗、供氢压力和回流能力是否满足电堆需求；阳极 lambda、惰性气体积累、purge 和液水管理哪些可观测、哪些仍待证；循环器/引射器能力是否有真实来源。
- **重点参数**：氢源压力/温度/组分、氢耗、供氢流量、阀压降/流通特性、循环器能力、分离效率、purge 逻辑和阳极压力边界。没有数据的循环/分离能力不得用经验公式补齐。
- **接口与场景**：承接电堆电流、温度和热管理边界；通过 `w` 接入氢源，通过 `u` 表达供氢/回流/purge 控制，通过 `y/z` 区分测量和审计真值；验证低/中/高负载下的供氢和水管理边界。
- **实施路径**：氢耗和压力需求账本 → 官方结构/方程来源核查 → 回流、排氮和液水观测能力判断 → 有数据时进行局部模块升级 → 与电堆负载和热状态联合回归。
- **阶段产出**：氢侧能力账本、阳极接口契约、回流/分离/purge 证据等级、可观测性缺口和适用工况边界。
- **退出条件**：氢耗和供氢压力需求闭合，已具备的回流/水管理能力通过最小验证，未具备阳极 lambda 或循环器数据的部分明确限制结论强度。

#### Stage 4：电气接口与辅电边界

- **目标**：在研究问题确实需要时，把电堆电气输出、受控负载、DCDC、母线和辅电消耗纳入系统级功率匹配。
- **系统对象**：电堆电压/电流/功率端口、受控负载、DCDC 边界、母线/辅电负载和电气测量；不默认扩展为完整电气系统。
- **核心问题**：堆端功率与负载请求如何匹配；辅件功耗是否影响净功率；DCDC 的效率、限值和动态是否有来源；电气边界是否会反过来改变空气、热管理和控制需求。
- **重点参数**：电堆 I-V-P 关系、负载轨迹、DCDC 功率/效率/限值、辅电负荷和电气控制约束。只有研究问题触发时才将 DCDC/辅电参数纳入 `P0/P1`。
- **接口与场景**：冻结现有电堆电气输出和受控负载契约；按需增加净功率、辅电和母线测量；验证负载变化、稳态功率平衡和必要的瞬态电气响应。
- **实施路径**：明确研究触发条件 → 电气端口和功率账本读回 → 来源和接口设计 → 只对有数据支撑的边界进行小步扩展 → 与气体/热管理回归。
- **阶段产出**：电气边界定义、堆端/净端功率账本、辅电影响、DCDC 纳入条件和最小验证方案。
- **退出条件**：电气设备责任和接口不再含糊，功率平衡和限制条件可验证；若研究问题不需要，则保持受控负载边界，不为完整性而扩展模型。

#### Stage 5：参数化、标定与验证闭环

- **目标**：把已闭合的系统模型转化为可复用的参数 profile，并通过数据和独立验证提高预测可信度。
- **系统对象**：`platform_default`、显式 `external_case`/profile、`P0/P1` 参数集、派生关系、观测 KPI、标定数据和验证数据；不是新的物理子系统。
- **核心问题**：哪些参数可由现有数据辨识；哪些参数高度相关或不可观测；训练/验证如何分离；误差、物理门槛和适用范围如何同时满足；何时需要局部高保真工具校核。
- **重点参数**：先从影响系统闭合和研究结论的 `P0/P1` 开始，按敏感性筛选 `P2`；`deferred` 参数只保留数据需求，不进入全量扫掠。
- **接口与场景**：保持同一模型和参数契约，外部 profile 只能显式接入；验证至少覆盖代表性稳态、切换瞬态和独立边界，记录误差、失效工况和不确定性。
- **实施路径**：数据审查 → 参数可辨识性/敏感性分析 → 训练/验证划分 → 受控拟合或方程参数化 → 系统级复验 → 必要时将局部空间机理结果以适用范围明确的参数/代理/ROM 回灌。
- **阶段产出**：可复用参数 profile、标定记录、验证报告、误差和不确定性边界、失效工况清单及高保真协同触发条件。
- **退出条件**：参数来源、拟合边界、验证边界和结论强度一致；模型不会因为低误差拟合而失去物理意义；可明确说明哪些能力已经验证、哪些仍待证。

控制与数值健壮性是贯穿各阶段的横向工作线，不单独等待所有 BOP 完成。每个阶段都采用“来源确认 → 参数/接口闭合 → 最小模型动作 → 结构读回 → 最小回归 → 决定是否扩展”的闭环。

### 0.4 顶层规划完成标准

本轮顶层规划的完成，不以已经修改 `.slx` 为标准，而以 Stage 1-5 均已明确目标、对象、核心问题、重点参数、接口/场景、实施路径、阶段产出和退出条件，并且这些阶段之间的依赖关系、横向控制/数值工作线和长期验证路线已经明确为标准。满足后，后续 Simulink 工作只需按当前路线进入相应专项，不再重新讨论项目总方向。

## 1. 实施顺序

### Phase 1：当前事实、资产与项目记忆清理

目标是先回答“当前究竟有哪些可用资产、哪些只是历史证据、哪些规则会污染主线”。本阶段只读盘点和分类，不改模型。

- 对模型、参数脚本、runner、当前说明、材料池、官方示例、外部案例和 `99_历史归档` 建立角色清单，分类为：当前真源、当前验证证据、材料参考、外部案例或历史归档。
- 逐份检查旧说明的适用版本、参数来源和结论强度；同类内容只保留一个当前入口，其余标记为归档候选，不再默认参与后续决策。
- 审核 Codex 项目记忆与工作流限制，分为保留、更新、删除三类。只保留稳定的模型边界、参数隔离、工具会话和产物纪律；删除已废弃路线、一次性仿真数值、过期文件路径、已否定的硬步骤和会把旧 10 kW/公司资料带回默认链的内容。
- 保持 `platform_default` 与 `external_case` 的隔离；历史外部案例、旧资料和旧标定只能作为显式外部案例或参考证据。

通过门：形成经读回核对的资产/说明/记忆分类表，明确唯一当前模型、参数源、研究入口、材料池入口和归档入口；不存在未标注却仍被视为当前真源的旧文件或记忆规则。

### Phase 2：工程化系统模型定义草案与对齐准备

目标是共同确定未来模型要服务的工程问题、边界和保真度，而不是提前补齐所有设备。成果写回现有工程化规格，不新增平行 Markdown。

- 固定“通用燃料电池系统集成设计与参数匹配”为总目标；当前以官方案例约 50 kW 数据作为最初参考尺度、参数锚点和验证载体，而不是把 50 kW 固定为最终应用规模。保留官方 Gas Mixture PEMFC 派生骨架和单模型/单参数源/单全工况入口原则。
- 定义工程化系统的四个域：供氧与 cEGR、供氢与阳极水管理、热管理、电气接口。明确每个域的目标设备、物理机理、必要测量、控制角色和本阶段不建模内容。
- 固定建模来源与表达范围：官方燃料电池系统案例、官方 FuelCell/Simscape 网络和模块级复用优先；官方资产未覆盖的内容才进入文献或明确物理化学方程路线。模型保持复杂的零维集总参数系统级定位：零维只表示不求解空间场分布，不表示模型简单。电堆、多域物理网络、BOP、控制器和参数匹配通过守恒/本构方程、常微分/代数关系、必要的微分代数求解、物理端口和变量传递耦合；不在 Route A 内承担几何结构或偏微分方程场建模。
- 明确研究场景不是系统能力边界：系统应允许独立调节进气量、温度、湿度、压力、回流比、恒电流/恒电压以及稳态/瞬态边界；实际研究只选代表性工况，不穷举全部参数组合。
- 固定代表性场景：无/低/中 cEGR 下的低/中/高负载稳态，以及一次无 cEGR-开 cEGR-无 cEGR 的切换瞬态。切换瞬态在编译有 cEGR 拓扑下通过目标回流比 `0 -> representative -> 0` 实现，禁止运行中切换 `routeA_cegr_enabled` 变体。
- 明确当前模型是系统级物理网络和方案筛选平台，不是机械总成或认证模型。
- 冻结 `u/w/y/z`、单位、时序、初始状态、`platform_default` 与 `external_case` 边界，以及“控制命令不等于物理能力证明”的验收原则。
- 对每个目标部件明确预期保真度、参数来源等级、可验证证据和升级触发条件；没有官方复用依据、文献依据或明确方程的设备责任保留为 L2 待证接口，不得用未经验证的 MATLAB Function 经验公式提前补齐。

通过门：形成一份工程化系统定义草案和待对齐清单，包括四域架构、设备清单、研究问题、保真度边界、参数来源层级、控制/观测接口和验收用途。草案可进入资产整理，但不得据此进入具体 BOP 改模或参数拟合。

### Phase 3：工程资产全面梳理、归档与当前真源收口

目标是在 Phase 1 分类和 Phase 2 定义草案的基础上，对现有工作资产做减法，形成清晰、可追溯且不相互矛盾的当前工作树。本阶段仍不改 `.slx`。

- 合并可合并的说明、脚本说明和审计入口；当前规范只保留模型规格、实施路线、材料池/候选比较、项目 `AGENTS.md` 和必要 runner。历史阶段文件、双模型比较、废弃方案和重复说明进入现有 `99_历史归档`，并保留简短索引。
- 对模型、参数源、全工况 runner、审计 runner、官方示例、材料池、外部案例和历史证据建立可追溯关系；不复制主模型，不把材料参考或外部案例伪装成当前默认资产。
- 按 Phase 1 的结论更新或删除 Codex 过期项目记忆，避免旧路线、旧探针和一次性结论继续约束新任务；只保留已验证的长期边界与必要入口。
- 建立一次 Git 可追溯基线；不提交 `slprj/`、`.slxc`、base workspace 结果或运行缓存，也不以清缓存作为前置动作。

通过门：当前工作树、说明真源、历史归档和 Codex 长期记忆彼此一致；后续顶层规划所需的模型、证据、材料和待确认问题都有唯一入口。未通过该门，不依据分散旧文件制定建模路线。

### Phase 4：后续建模顶层规划设计与共同对齐

目标是以已整理的资产和工程化定义草案为输入，与用户共同决定“未来具体建什么、为何建、先建什么、保持什么不变、如何验证”。这是防止后续 Simulink 建模偏离研究目标的最终对齐步骤，不是启动改模的授权。

- 按五项工程化准则审视当前 Route A：官方案例派生与物理网络真实性；参考尺度电堆与 BOP 参数闭合；控制逻辑与代表性研究场景支持能力；子系统布局、变量耦合和可维护性；参数化、拟合和验证的复用边界。
- 将建模来源作为结构性审查门：每个设备模块必须能够追溯到官方案例/官方模块复用，或追溯到文献和明确物理化学方程；仅由 MATLAB Function 经验公式构成的等效设备模块不得进入当前主模型。
- 将现有能力、L2 待证能力和阻断性缺口分别列出，并逐项标明“已验证事实、基于证据推断、待确认假设”。已有九工况和参数审计只能作为证据输入，不可扩大为设备能力或系统应用结论。
- 设计未来模型的顶层演进方式：四域子系统的目标边界和复用关系，单模型/单参数源/单入口如何维持，边界参数和设备参数如何配置，何时需要参数拟合或局部高保真校核。
- 共同确定后续专项的优先级和边界。每个候选专项必须说明目标部件、允许/禁止改动、所需数据、参数来源、接口影响、最小验证、退出条件和与其他子系统的隔离方式。
- 将尚需用户决策的内容显式保留，例如设备保真度、边界参数范围、可获得的部件数据、参数拟合目标和验收强度；不由 agent 擅自假定。

当前规划建议按以下顺序推进：电堆核心与供氧/cEGR 联合参数匹配及接口闭合；热管理参数匹配与热控制闭合；供氢与阳极水管理参数匹配；电气接口与辅电边界。该顺序只表示证据和耦合关系上的优先级，不代表自动修改模型。

#### 首个专项工作包：电堆核心与供氧/cEGR 联合参数匹配及接口闭合

- **对齐状态**：第 1 批已对齐；首轮保留电堆内部结构和机理不变，只做来源、参数、反应/热/电账本及 BOP 匹配核查。
- **目标**：以电堆为核心锚点，闭合其与供氧/cEGR 域的流量、组分、压力、湿度、回流和控制接口，形成模块来源、参数能力和验证缺口的单一证据链。
- **对象**：官方气体混合电堆网络、MEA/阴阳极气道、反应/电压/热/电气端口及关键参数链；空气源与压缩机流量源/地图接口、中冷、阴极加湿、cEGR 阀/回流管、排气/背压、水分离、入口混合、相关传感器及空气/cEGR 控制链。
- **来源门**：优先官方案例与官方 Simscape/Simulink 模块级复用；官方未覆盖部分才采用文献或明确物理化学方程。没有上述依据的内容保留为 L2 或 `deferred`，不得用经验性 MATLAB Function 等效设备。
- **第 2 批对齐状态**：所有模块统一登记为 `official_reuse`、`equation_based`、`l2_interface` 或 `deferred`。MATLAB Function 仅可承担控制、逻辑、参数装配、代数换算和诊断；经验公式设备等效不作为当前物理证据，暂不直接删除，后续凭正式来源或方程单独替换。
- **允许内容**：只读结构与参数读回、参数注册和单位核查、电堆反应/电压/热/电账本、空气/cEGR 能力账本、接口契约整理、现有代表性矩阵证据整理和数据缺口定义。
- **禁止内容**：改 `.slx` 或电堆内部机理、改 `platform_default`、改控制律或 runner、增加设备等效模块、引入空间场模型，以及把 OER/回流命令当作压缩机、阀、加湿器或分离器能力证明。
- **最小验证**：电堆账本和 BOP 来源/参数状态齐全；电堆需求与质量/组分/压力/RH/热量计算有限且量纲正确；无 cEGR 回流为零；低/中 cEGR 跟踪、压差方向和阀面积边界正确；`lambda_ca_in` 满足门槛；请求流量与压缩机地图有效范围一致；无数据能力项明确标为待证。
- **隔离与退出**：电堆内部结构和机理保持不变，冻结供氢、热管理和电气接口，只读取其边界需求。完成后输出电堆-供氧/cEGR 专项缺口和最小变更建议，是否进入 Simulink 修改必须另行授权。

#### 第 3 批已对齐：接口契约实施门

`u/w/y/z` 的语义已对齐，但具体端口级实现仍须在进入 `.slx` 改动前只读读回确认。读回项目包括名称、单位、方向、连续/离散属性、变量所有权、物理端口与信号线关系以及可观测性；无法读出的量登记为 `unobservable`，不以新增物理模块补齐。`routeA_cegr_enabled` 仍是更新模型时生效的编译期配置，不是运行时 `u`。

#### 第 4 批已对齐：参数重点与数量控制

参数采用 `platform_default`、显式研究场景、统一派生量和 `external_case` 四层管理。当前主动研究集只纳入 `P0/P1` 参数：`P0` 负责电堆-BOP 守恒、接口闭合和验收，`P1` 负责控制与代表性稳态/切换瞬态；`P2` 仅在敏感性或异常诊断需要时启用，`deferred` 只登记来源和数据缺口。参数不以全量扫掠为目标，而以是否能改变当前研究结论或验收结果作为纳入标准。

#### 第 5 批已对齐：统一模型与分级验证

无 cEGR、有 cEGR 和切换瞬态统一使用同一个 `.slx`、同一个 `platform_default` 和同一套参数装配/runner。编译期拓扑选择用于隔离结构验证；需要共享完整运行态初值的性能对照则保持 cEGR 拓扑开启，以目标回流 `0` 和显式目标回流比表达参考/试验差异，不复制模型、参数或隐藏调参链。验证先做中负载三工况，再扩展低/高负载稳态，最后做编译有 cEGR 拓扑下的 `0 -> 0.10 -> 0` 切换瞬态；每一级通过后才进入下一级。

#### 第 6 批已对齐：原则冻结、实现可演进、变更可审计

保持一个主 `.slx`、一个 `platform_default`、一套 runner、稳定的物理守恒和 `u/w/y/z` 语义；允许在发现结构/物理/控制/数值问题，或获得可靠官方模块、文献方程和数据时进行受控修改。每项修改必须说明动机、证据、影响、保持不变项、最小回归和回滚依据；影响接口、默认参数、控制行为或验证结论的修改必须单独回归。

#### 六批整合后的 Simulink 进入门

六批规划已经共同对齐，但正式改模前仍需完成：端口级只读读回；电堆-供氧/cEGR 首个专项的来源、参数和接口预检；基线 Git 提交；以及用户对首个实施提交的单独授权。通过这些门后，仍按“读回—小步修改—`model_check`—端口读回—最小回归”的闭环执行。

通过门：用户与 agent 共同确认一份后续建模顶层规划，包含研究目标、系统边界、子系统演进顺序、参数与验证路线、明确不变项、待确认项和每个专项的验收门。此门通过后，是否启动第一个 Simulink 专项仍由用户单独授权。

## 2. 规划阶段门禁

| 门 | 本轮必须回答的问题 | 禁止越界 |
|---|---|---|
| 事实门 | 当前模型、参数、脚本、说明、材料、记忆各自是否有唯一角色？ | 不依据旧文件或记忆直接改模型 |
| 定义门 | 通用系统集成设计研究什么、不研究什么、以何证据验证？ | 不把系统级平台表述为完整机械或电气系统，不把经验公式等效模块当作物理设备 |
| 归档门 | 当前工作树、说明、材料和记忆是否已收口为可追溯的唯一入口？ | 不依据分散旧文件或旧记忆制定建模路线 |
| 对齐门 | 后续顶层规划是否经共同确认，并保留所有未决选择？ | 不自动批准模型改动、不复制主模型、不污染 `platform_default` |

## 3. 进入 Simulink 后的持续验收门

| 门 | 最小证据 |
|---|---|
| 结构门 | 变体路径、关键物理连接、参数引用和子系统接口读回正确 |
| 设备匹配门 | 每个关键 BOP 都有参数来源、能力范围和相对电堆需求的闭合计算 |
| 控制门 | 无/低/中 cEGR 的低/中/高负载代表性稳态，以及一次目标回流比 `0 -> representative -> 0` 切换瞬态完成；实际 `lambda_ca_in`、压力、RH、温度和执行器约束通过 |
| 数值门 | 冷/热起动、负载阶跃和初始状态扰动不产生未解释的初始化或零交叉失败 |
| 应用边界门 | 外部数据不污染 `platform_default`；拟合与验证工况分离，结论强度与证据一致 |

## 3.1 Stage 1 首轮执行记录（2026-07-16）

本轮先按“只读读回—需求账本—接口闭合—代表性稳态验证—缺口分类”完成基线核查；在用户单独授权后，继续实施时变 EGR 目标输入接口和切换瞬态验证。稳态核查阶段未修改电堆机理、默认控制语义、全工况 runner 或公共 `u/w/y/z` 接口；本次受控变更仅增加内部 profile 选择路径并调整局部数值诊断设置。`slprj/`、`.slxc` 和运行缓存按规则保留。

### 入口与工具链

- 执行开始时 Git 工作树干净，`HEAD` 与 `origin/master` 均为 `92532f7`；本次完成后的未提交变更均为 Stage 1 实施记录、参数源、主模型和专项审计脚本，未混入旧案例资产。
- Codex MATLAB MCP 已完成模型读回；模型配置为 variable-step、`SignalLogging=on`、`SimscapeLogType=all`。
- 模型工作区读回为 `routeA_parameter_layer=platform_default`、`routeA_external_case_enabled=false`、`routeA_cegr_enabled=true`。
- 当前研究输入使用实际变量名 `routeA_target_egr_ratio_comp_in`；`routeA_cegr_enabled` 仅在独立仿真前用于 Variant/update-diagram 选择。

### 结构与接口读回

- `Stack_Core` 的 MEA、阳极/阴极气道、阴极出口容腔和出口阻力连接完整；scoped `model_check` 为 healthy。
- `Cathode_Air_cEGR_BOP` 的空气/cEGR、阀前后压力传感器、EGR 管路和入口混合接口已读回。
- `Cathode_Exhaust_Backpressure_Water` 的 EGR 流量、尾气流量、出口压力/RH/温度/组分和水分离 KPI 接口已读回。
- `System_Control_Observability` 和 `cEGR_Mode_Selector` scoped `model_check` 为 healthy；根层检查的 70 个 warning 不作为零 warning 门槛。
- scoped warning 数量为：`Cathode_Air_cEGR_BOP` 4、`Cathode_Exhaust_Backpressure_Water` 5、入口质量流量传感器 5、阳极支路 22；`Stack_Core`、`System_Control_Observability`、`Thermal_Management_BOP` 和 `cEGR_Mode_Selector` 均为 healthy。其余根层 warning 是层级边界/物理端口检查重复报告，不能与 scoped 数量直接相加。
- Stage 1 相关 warning 已分类为层级物理端口检查误报、未使用的湿度/质量流量观测端口，以及排气管可选 MIn/TIn 端口；未发现阻断当前阴极气路的断线。
- 阳极支路的历史 L2 观测/未连接端口 warning 不纳入本专项结论，不通过新增观测块掩盖。

### 需求账本与参数状态

| 负载 | 目标功率 (kW) | 电堆热需求 (kW) | 新鲜空气需求 (kg/s) | cEGR 0.10 screen (kg/s) | cEGR 0.30 screen (kg/s) |
|---|---:|---:|---:|---:|---:|
| low | 17.472 | 15.680 | 0.031767 | 0.035297 | 0.045382 |
| nominal | 50.960 | 65.072 | 0.083389 | 0.092654 | 0.11913 |
| high | 77.952 | 120.960 | 0.095301 | 0.10589 | 0.13614 |

电堆反应/热/电需求账本有限且量纲正确。`stack_num_cells`、`stack_area`、MEA 参数和控制参数登记完成；cEGR 阀上限及 PI 面积参数通过 nominal 诊断。压缩机 map、intercooler、separator、冷却泵和散热器能力继续保持 `L2_pending`/`deferred`，不宣称机械效率、轴功率、喘振、真实分离效率或真实散热能力。

### 稳态验证结果

现有 `run_routeA_fullcase_study.m` 和 `run_routeA_phase1_matching_audit.m` 均通过 Code Analyzer，九工况无跳过、无失败：

| 工况 | 实际功率 (kW) | 实际 cEGR | `lambda_ca_in` 最小值 | 结果 |
|---|---:|---:|---:|---|
| nominal noCEGR | 50.960 | 0 | 2.99812 | pass |
| nominal cEGR 0.10 | 50.960 | 0.0999932 | 2.86806 | pass |
| nominal cEGR 0.30 | 50.960 | 0.299974 | 2.48871 | pass |
| low/high 其余 6 工况 | 17.472/77.952 | 0/0.10/0.30 | 1.54001–3.99786 | 6/6 pass |

匹配审计汇总为 `matrixPassed=true`、`reactionFinite=true`、`registryComplete=true`、`passivePassed=true`、`blockingGaps=0`、`engineeringMatchReady=true`。被动 cEGR 诊断结果：0.10 工况压力差 `0.061046 MPa`、阀面积上限占比约 `0.236`；0.30 工况压力差 `0.060886 MPa`、阀面积上限占比约 `0.731`；两者均为正向压差且未饱和。

### 时变目标接口实施与切换瞬态

用户授权后，在同一主模型和同一 `platform_default` 参数源内完成了 `FCU_BoP_Control` 的内部目标输入选择路径：

- `routeA_egr_target_input_mode_id=0` 保持原有标量 `routeA_target_egr_ratio_comp_in`；`=1` 选择 `routeA_target_egr_ratio_comp_in_profile`，profile 格式为 `[time_s, target_ratio]`。
- 新增的 `From Workspace`、模式 Constant 和 Switch 均位于 `FCU_BoP_Control` 内，Switch 输出接入原 `EGR Ratio Error`；公共 `u/w/y/z`、PI 增益、PI 误差方向和直接面积模式保持不变。
- 本次切换仿真把 `routeA_cegr_enabled=true` 固定在 update-diagram/编译阶段，运行中没有切换 Variant；目标 0 只是 PI 将 `EGRValveRestriction` 面积压到 `cegr_valve_min_area` 附近。`routeA_cegr_enabled=false` 仍作为独立的拓扑隔离检查；当前同初值性能对照则保持 `routeA_cegr_enabled=true`，以目标 0 作为零实际回流参考，不能解释成运行中物理气路快速断开/接入。
- profile 使用 `Interpolate=off`、`OutputAfterFinalValue=Holding final value`；默认 held profile 覆盖模型 2500 s 时域，正常运行审计 profile 覆盖逻辑 600 s。当前 `SampleTime=-1` 继承模型步长，运行时产生“source specifies -1 sample time”非阻断性诊断；独立控制采样周期待后续接口契约冻结后再显式设置。
- 首次时变运行暴露了 `Abs egr mdot` 和 `EGR Ratio PI` 的零交叉失败；在保持控制律和信号语义不变的前提下，将这两个局部数值诊断设为 `ZeroCross=off`，随后通过结构检查、瞬态和稳态回归。
- `FCU_BoP_Control` 的 `model_read` 读回显示 profile、mode、static fallback 三路均连接到目标选择器；scoped `model_check` 为 `healthy`。同时使用 MATLAB 图形化模型截图进行视觉复核，未发现新增悬空线、错误端口或层级遮挡；原有阀面积输出附近日志标签较密，但未由本次接口引入且不造成语义歧义，留作独立布局整理项。视觉检查作为布局/连线辅助证据，不替代 API 读回和结构检查。

早期切换 smoke 审计使用 `run_routeA_stage1_cegr_transition_audit.m` 的旧 120 s 配置，编译期保持 cEGR 拓扑开启，窗口为 pre `[10,19] s`、mid `[40,69] s`、post `[90,119] s`。这些窗口包含堆从约 `37 °C` 开始的暖机过程，不再作为 CEGR 性能因果或回稳态验收证据：

| 窗口 | 目标 cEGR | 实际均值 | 平均绝对误差 | 尾段 span |
|---|---:|---:|---:|---:|
| pre | 0 | `4.81572e-7` | `4.81572e-7` | `8.37819e-9` |
| mid | 0.10 | `0.0975614` | `0.00243859` | `0.00620762` |
| post | 0 | `0.00382354` | `0.00382354` | `0.00739527` |

该 smoke 运行的功率、氧计量、压力方向和有限性门通过，但其 pre/mid/post 读数不能直接解释为 CEGR 对稳态系统性能的影响。全时域 derived `lambda` 在无负载初始化点有 1 个非有限样本，已单独记录并排除在实际负载窗口门外；这不被解释为电堆/BOP 运行期失效。

#### 早期暖机样本的系统影响读回（不作为性能门）

该审计保持 `drive_cycle_power=[0,50.96,50.96] kW`，0.5 s 后功率请求保持 50.96 kW；空气侧为 `target_oer=3`，因此是“功率基本不变、电流随电压调整”，不是固定电流试验。三窗口读回如下：

| 窗口 | 电流 (A) | 电压 (V) | cEGR (kg/s) | 压缩机总流量 (kg/s) | 新鲜空气近似 (kg/s) | 阴极入口 O2 质量分数 | 压缩机 cmd/rpm |
|---|---:|---:|---:|---:|---:|---:|---:|
| pre | 127.490 | 399.722 | `2.59e-8` | 0.0538782 | 0.0538782 | 0.220141 | 0.348206/1253.54 |
| mid | 125.385 | 406.430 | 0.00520299 | 0.0533321 | 0.0481291 | 0.206259 | 0.347720/1251.79 |
| post | 124.849 | 408.172 | 0.000203003 | 0.0530918 | 0.0528888 | 0.215561 | 0.343351/1236.06 |

mid 相对 pre 的直接读回为：电流 `-2.105 A`、电压 `+6.708 V`、阴极入口 O2 质量分数 `-0.013882`、cEGR 流量 `+0.005203 kg/s`、新鲜空气近似 `-0.005749 kg/s`、阴极入口总流量 `+0.000714 kg/s`；RH_in `0.708109 -> 0.671455`，RH_out `0.461345 -> 0.355576`，水分离观测 `0.0122047 -> 0.0149327`，尾气流量 `0.0574182 -> 0.0530783 kg/s`。压缩机入口压力保持 `101325 Pa`，入口温度 `293.15 -> 299.364 K`；当前压缩机是流量源/地图接口，不能据此宣称轴功率或效率变化。

模型同时读回了阴极入口物种流量向量和压缩机入口 `y_i` 向量。当前审计正式确认第 2 个组分通道为 O2；其余通道的官方物种顺序还没有写入接口元数据，因此本轮保留向量数值，不把第 1/3/4 通道擅自命名为具体物种。EGR 上升 10--90% 为 `19.4941 s`，下降 90--10% 为 `18.0074 s`，说明当前接口是慢调节响应，不能作为快速物理隔离能力证明。

noCEGR 独立编译隔离基线的 late 窗口为：电流 `124.840 A`、电压 `408.202 V`、压缩机流量 `0.0530803 kg/s`、cEGR `0`、阴极入口 O2 质量分数 `0.215955`。它与 transition mid 窗口的差异还叠加了不同时间段的堆温/水热状态，不能直接当作纯 EGR 因果增量；后续需要同初始状态或同热状态的成对稳态/瞬态试验，才能形成可归因的 CEGR 性能影响结论。

#### 恒功率边界与 pre/mid/post 解读

以下内容只解释旧 120 s 暖机 smoke 的方向反转，不作为当前 Stage 1 的性能验收；稳态锚定结果见后文。

当前 `Electrical Load` 的活动输入模式是 `Drive cycle`。`Drive Cycle Power Demand` 从 `[drive_cycle_time, drive_cycle_power]` 读入功率需求，经 `PS Divide` 按 `i=P/v` 生成受控电流源命令；因此 `drive_cycle_power=[0,50.96,50.96]` 是外部负载功率请求，不是电堆内部独立的电压或电流控制器。

- pre 到 mid 中，O2 质量分数由 `0.220141` 降至 `0.206259`，cEGR 增加，但堆温也由 `36.86` 升至 `71.41 °C`，且恒功率边界下电流由 `127.490` 降至 `125.385 A`。降低电流会降低极化损失，升温会改善当前堆模型中的反应/欧姆项；这两个作用在本次运行中超过了 O2 降低的负作用，所以电压由 `399.722` 升至 `406.430 V`。由 `P=VI`，电压升高必然伴随电流降低；不能把该结果解释为 cEGR 单独提高了电压。
- 当前模型已有 `Ramp Current Demand` 和 `Step Current Demand` 两个电流需求变体，可以开展给定电流轨迹下的试验；但 Stage 1 尚未建立与 cEGR 事件联动的恒流审计入口。恒流下应直接读回 `P=VI`，若同热状态下 cEGR 使电压降低，功率将随之降低。
- 当前模型没有恒压闭环。`Voltage Sensor` 的读数用于功率需求的 `P/v` 换算和观测，没有电压误差控制器；恒压研究需要单独授权增加电压控制接口。若同热状态下保持电压而 cEGR 使可维持电流降低，功率才会相应降低，方向不能由本次恒功率结果直接代替。
- post 的目标已经回到 0，但过渡仿真始终保持 `routeA_cegr_enabled=true`，没有运行中切换 Variant。PI/执行器只能把阀面积压到 `cegr_valve_min_area=1e-10 m²`，并且 EGR 管路/腔体和控制器仍有状态记忆；因此 post 仍有 `0.000203 kg/s` cEGR、实际比约 `0.003824`。当前下降 90--10% 时间约 `18.01 s`，post 窗口还包含未完全衰减的过程。
- post 继续出现电压升高、电流降低，主要是堆从初始冷态继续升温和状态演化，而不是“无 cEGR 后没有回到 pre”。transition post 的堆温为 `80.135 °C`、电压 `408.172 V`；独立 noCEGR baseline 的 post 为 `80.136 °C`、`408.202 V`，两者已接近。真正的 noCEGR 因果对照应在相同热/压力/湿度/控制器状态下进行，不能拿早期冷态 pre 窗口作为回归目标。

#### 正常运行态初值与恒流切换复验（2026-07-16）

本阶段不以冷启动作为性能研究起点。唯一的 `platform_default` 初值保存在 `01_模型/RouteA_GasMixture_Derived/RouteA_platform_default_initial_state.mat`：它是完整的 `Simulink.op.ModelOperatingPoint`，而非只写入几个温度或压力标量；因此同时携带堆温、气体库存、湿度、压力、流量、组分和控制器内部状态。生成工况为 `j=0.1 A/cm^2`、`A=280 cm^2`、恒 `I=28 A`、`routeA_cegr_enabled=true` 且目标回流为零，实际回流约为零。状态快照模型时间为 `4823.57193 s`，取阳极最近一次吹扫后 `100 s` 的相位；该运行点的阳极吹扫周期约 `789.807 s`。后续每个正常运行研究都从这同一状态重新起算，不以某一试验的结束状态作为下一试验初值。

全状态初值要求保持 cEGR 物理拓扑开启，故零回流参考不再编译成 `routeA_cegr_enabled=false` 的另一拓扑，而是在同一拓扑下将目标回流保持为零。审计以保存快照为逻辑 `t=0`，模型绝对时间仅用于承接 operating point；研究 profile 为逻辑 `0--60 s` 零回流、`60.01--240 s` 目标 `0.10`、`240.01--600 s` 回零。pre/mid/post 分别为逻辑 `[30,59] s`、`[210,239] s`、`[540,599] s`，600 s 窗口短于下一次吹扫，避免吹扫事件直接落入 CEGR 切换段。

| 窗口 | 电流 (A) | 电压 (V) | 功率 (kW) | 堆温 (°C) | 实际 cEGR (kg/s) | 阴极入口 O2 质量分数 |
|---|---:|---:|---:|---:|---:|---:|
| pre | 28.000 | 427.740 | 11.9767 | 80.0000 | `2.50e-8` | 0.215863 |
| mid | 28.000 | 425.490 | 11.9137 | 80.0005 | 0.00166679 | 0.206004 |
| post | 28.000 | 424.830 | 11.8952 | 80.0002 | `2.29e-8` | 0.215864 |

实际回流比在三窗分别约为 `1.50e-6`、`0.100000`、`1.37e-6`；上升 10--90% 为 `8.03283 s`，下降 90--10% 为 `7.21041 s`。恒流跟踪最大误差为 `0 A`，`lambda_ca_in` 最小值为 `4.05355`，阀前后中段压差为 `0.0603002 MPa`，阀面积占允许范围为 `[1.02e-6, 0.07385]`；有限值、压力方向、阀限和各窗口局部稳态门均通过。

同一运行内 post 相对 pre 的电压差为 `-2.91057 V`，不能解释成“关闭 cEGR 后仍有 2.91 V 残留”。从同一初值、同一逻辑时刻运行的零回流参考轨迹本身 pre-to-post 也变化 `-2.91744 V`，这是阳极氮气库存沿吹扫周期演化造成的时间项。正确的控制变量比较为同一逻辑时刻的成对差：mid 相对匹配零回流参考为 `dI=0 A`、`dV=-1.14101 V`、`dP=-0.0319482 kW`、`dT_stack=+0.000168 °C`、`dO2mf=-0.00985871`、`d(cEGR)=+0.00166677 kg/s`、`d(fresh air)=-0.00166677 kg/s`、`d(inlet total)=+0.000228291 kg/s`。回零后的 post 相对匹配参考为 `dV=+0.0068693 V`、`dO2mf=+8.49e-7`、`dT_stack=-3.22e-5 °C`，匹配回归门通过。

此前把 Composition and Humidity Sensor 的四组分输出整体展平后得到的 `RH` 变化和跨度无物理含义。后续只使用水蒸气分量 `W(4)` 作为相对湿度：本次 pre/mid/post 的阴极入口 RH 分别为 `0.986110`、`0.983827`、`0.986112`，出口水蒸气 RH 约为 `1.00105`、`1.00103`、`1.00105`。水分离观测仍只作为 L2/KPI，不被解释为真实分离效率。

#### 本轮方法论总结：周期运行性能与运行态初值

本轮的首要结论不是“系统没有稳态”，而是该系统在存在阳极氮气累积和吹扫控制时，不能用所有变量严格恒定的静态点定义正常工作性能。电压随时间的周期性变化与阳极库存/吹扫事件相符；只要研究边界、初始相位、研究时长和统计窗口保持一致，它就是可解释、可对照的系统运行行为。

因此，Stage 1 后续稳态性能研究采用固定时长的末段统计而不是单个末值：对当前 `j=0.1 A/cm^2` 首轮矩阵，所有案例从同一个保存运行态初值独立启动，运行逻辑 `600 s`，以末段 `[540,600) s` 的均值作为边界条件下的性能代表值，并同步记录该窗口的 span/标准差。cEGR、气、热、湿、压等任一边界的作用，均以同一逻辑时间窗口、同一初值、零实际回流参考的均值相减获得；运行内 pre-to-post 差只保留为库存/周期演化诊断。

本轮的第二个结论是，初始值必须服务于研究问题。对于暂不研究冷启动的正常性能矩阵，冷态环境初值会把升温、加湿、气体库存建立等长暂态混入计算，既降低计算效率，也破坏控制变量法。保存的完整 operating point 使温度、湿度、压力、流量、组分和控制器状态从正常运行相位开始；冷启动只在未来作为独立专题启用。`0 -> 0.10 -> 0` 试验的作用是证明模型和接口具备瞬态研究能力，不能替代上述稳态性能统计。

#### 气体条件优先读回与守恒审计（2026-07-16）

本轮先固定四处气体条件，再解释温度、压力和电性能。模型当前阴极组分向量按 `[N2,O2,H2,H2O]` 读取；第三项在阴极侧为数值零附近。四处量的证据等级如下：

| 位置 | 流量 | 组分 | 证据等级 |
|---|---|---|---|
| 阴极入堆 | `CathodeInletMassFlowSensor_FC M_i` 直接给出各组分质量流量 | 直接质量流量读回，并计算质量分数 | direct |
| 阴极出堆 | cEGR 与排气两个分支质量流量绝对值之和 | `CathodeOutletChamber yC_i -> routeA_yi_outlet` 直接给出 `y_i`，按摩尔质量换算质量分数 | flow derived / composition direct |
| 循环支路 | `EGRMassFlowSensor M` 直接给出 | 当前无支路 `M_i` 转换器，按出口 `y_i` 推导；SeparatorOrCondensation 仅为观察器 | flow direct / composition derived |
| 外界新鲜空气 | 混合总流量减循环流量 | `Air Intake y0` 是模型工作区边界组分 | flow derived / composition direct boundary |

600 s 恒流三个窗口的总流量读回为：

| 窗口 | 入堆 kg/s | 出堆 kg/s | 循环 kg/s | 外界新鲜空气 kg/s |
|---|---:|---:|---:|---:|
| pre | 0.0180569 | 0.0180318 | `2.50e-8` | 0.0166679 |
| mid | 0.0182852 | 0.0182844 | 0.00166679 | 0.0150011 |
| post | 0.0180567 | 0.0180656 | `2.29e-8` | 0.0166679 |

N2/O2 守恒首轮通过：mid 窗口 N2 入堆-出堆残差为 `4.41e-5 kg/s`；O2 净消耗为 `9.28554e-4 kg/s`，法拉第预测为 `9.28604e-4 kg/s`，残差为 `-4.95e-8 kg/s`。pre/post 的 O2 法拉第残差分别为 `-1.07e-7` 和 `-3.71e-8 kg/s`；三窗口的混合 N2/O2 残差均在当前 `5%` 相对或 `5e-4 kg/s` 绝对门内。因此，当前恒流 CEGR 影响分析的第一层气体证据已闭合：mid 的循环流量增加、外界新鲜空气流量下降、入堆 O2 质量分数从 `0.215863` 降至 `0.206004`，并且 O2 消耗与恒 28 A 满足法拉第关系。窗口平均残差不代替包含气体库存导数的瞬态全局质量平衡；后者需在后续专项中显式纳入库存项。

这不等于四处气体条件已经全部具备独立传感器：循环组分和外界流量仍是推导量；若后续要把“支路级组分守恒”提升为独立接口验收，需单独授权增加支路 `M_i` 转换器和外界进气质量流量观测，不能在当前审计中把推导量改名为 direct。

恒流性能研究不把目标功率替代为电流密度语义：串联电堆的 `I_stack=j*A`，`N_cell` 进入 `P_stack=N_cell*I_stack*V_cell` 及反应物消耗。`j=0.1 A/cm^2`、`A=280 cm^2` 时，恒流命令为 `28 A`，不是 `28*400 A`；上述切换已验证该控制边界。下一步恒流 cEGR 比矩阵仍以该唯一初值为起点，以实际 cEGR 比为横坐标、电压为主纵坐标，并记录功率、堆温、O2/lambda、流量、压力、RH、水分离观测和压缩机命令；每个回流比在固定 `600 s` 研究时长的末段 `[540,600) s` 取均值，并与同一逻辑时刻的零回流参考相减，不预先假定电压随循环比严格单调。恒压闭环仍是控制接口缺口，尚未宣称具备。

修改后的 `run_routeA_fullcase_study.m` 九工况仍全部通过；`run_routeA_phase1_matching_audit.m` 汇总仍为 `matrixPassed=true`、`reactionFinite=true`、`registryComplete=true`、`passivePassed=true`、`blockingGaps=0`、`engineeringMatchReady=true`；`run_routeA_platform_demo.m` 结果为 `passed=1`、`finite=1`。

### 未闭合项与下一道门

时变 EGR 控制输入接口、唯一正常运行初值和恒流代表性瞬态已闭合；四处气体流量/组分的首轮读回、N2/O2 守恒、O2 法拉第消耗以及相位匹配的回零门均已通过。运行内 post-pre 漂移保留为阳极周期状态诊断，不再作为 CEGR 回归门。当前 `SampleTime=-1` 诊断不阻断结果，但这不等于 cEGR 物理气路已实现运行中快速分离/接入。后续 Stage 1 收口还需补做恒流 cEGR 比稳态矩阵、完整水管理、真实隔离执行器/泄漏/库存释放专项验证和恒压接口设计。压缩机效率/轴功率/喘振、压比覆盖、阳极 lambda/purge、分离效率和冷却设备能力继续保持 `L2_pending`、`unobservable` 或 `deferred`。

本轮结论：Stage 1 已完成“控制接口闭合 + 九工况稳态基线 + 唯一正常运行初值 + 恒 28 A 代表性 cEGR 切换 + 四处气体条件及 N2/O2 守恒首轮读回 + 相位匹配 CEGR mid-effect/回零首轮读回”。恒流 cEGR 比性能矩阵、恒压接口和真实物理快速隔离仍未完成，因此不宣称 Stage 1 的完整 CEGR 稳态/瞬态联合匹配已经闭合。

## 4. 文件维护规则

1. 本路线、工程化规格和材料池分别负责阶段门禁、模型目标边界和材料/候选依据；不新增同职责说明文件。
2. 一个模型、参数或控制改动必须在同一 Git 提交中更新当前规格或验证记录；规划阶段的文档/归档变更不得伪装成模型验证。
3. 阶段脚本、旧方案和旧说明进入 `99_历史归档` 后不作为默认入口，不参与后续设计决策，除非专题追溯明确引用。
4. `slprj/`、`.slxc` 和活跃运行缓存默认保留且不纳入 Git；不以清缓存作为审计、提交或建模前置动作。
5. 对 `.slx` 的结构或布局变更，除 `model_read`、`model_check` 和参数读回外，补做一次目标子系统的 MATLAB 图形化视觉复核，检查悬空线、端口方向、层级遮挡和布局可读性；视觉证据用于发现图形问题，不替代 API 结构和行为验证。
6. 主模型保存纪律：`PEMFuelCellSystem_GasMixture_cEGR_RouteA_v01.slx` 是唯一当前模型事实源；`.slx.autosave` 只作 MATLAB 异常恢复文件，`.slxc` 与 `slprj/` 只作生成缓存，`PEMFuelCellSystemWithACustomLibraryParameters.m` 和 `PEMFuelCellSystemWithACustomLibraryDriveCycle.mat` 是参数/工况依赖，不是第二个模型。模型问题采用一个小步闭环完成：定位/修改 → 正式保存主模型 → 读回、`model_check` 和最小回归；这三项验证属于修改过程的一部分，不因后续单纯保存动作重复执行。纯布局或日志变更按风险做必要的读回/视觉确认，不自动触发完整行为回归。发现工作区为 dirty 时，先保留恢复副本再关闭或重载模型。2026-07-16 11:25 的主模型已正式保存并读回确认，当前不依赖 autosave 或缓存恢复。长时间审计脚本必须在结束时显式恢复模型，避免临时日志标记污染主模型。
